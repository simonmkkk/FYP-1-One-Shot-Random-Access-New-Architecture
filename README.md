# One-Shot Random Access 模擬與分析系統

**版本**: 2.0.0

論文復現項目：基於多信道時隙 ALOHA 系統的單次隨機接入建模與估計

## 📁 項目結構

```
FYP-1-One-Shot-Random-Access-New-Architecture/
│
├── main.py                        # 🎯 主程式入口 (CLI + 互動式選單)
├── pyproject.toml                 # 項目配置與依賴管理
├── uv.lock                        # 依賴鎖定文件
├── .python-version                # Python 版本指定
├── README.md                      # 項目說明
├── SIMULATION_OPTIMIZATION.md    # 模擬優化文檔
│
├── config/                        # ⚙️ 配置文件
│   ├── __init__.py
│   ├── loader.py                      # 配置載入器
│   ├── analytical/                    # 解析模型配置
│   │   ├── figure1.yaml               # Figure 1 配置 (Figure 2 共用此配置)
│   │   └── figure345.yaml             # Figure 3, 4, 5 合併配置
│   └── simulation/                    # 模擬配置
│       ├── figure345.yaml             # Figure 3, 4, 5 合併模擬配置
│       └── single_point.yaml          # 單點測試配置
│
├── analytical/                    # 📐 解析/數學模型模組
│   ├── __init__.py
│   ├── formulas.py                    # 論文公式 (Eq. 1-10)
│   ├── theoretical.py                 # 理論計算 (多周期迭代)
│   └── figure_analysis/               # 各圖表解析計算
│       ├── __init__.py
│       ├── figure1_analysis.py        # Figure 1 解析
│       ├── figure2_analysis.py        # Figure 2 解析 (誤差分析)
│       └── figure345_analysis.py      # Figure 3, 4, 5 合併解析
│
├── simulation/                    # 🔬 模擬模組
│   ├── __init__.py
│   ├── core/                          # 核心模擬引擎
│   │   ├── __init__.py
│   │   ├── one_shot_access.py         # 單次接入模擬
│   │   ├── group_paging.py            # 群組尋呼模擬 (多樣本)
│   │   └── metrics.py                 # 性能指標計算
│   └── figure_simulation/             # 各圖表模擬
│       ├── __init__.py
│       └── figure345_simulation.py    # Figure 3, 4, 5 合併模擬
│
├── plot/                          # 📊 繪圖模組
│   ├── __init__.py
│   ├── common.py                      # 共用繪圖設定
│   ├── figure1.py                     # Figure 1 繪圖
│   ├── figure2.py                     # Figure 2 繪圖
│   └── figure345.py                   # Figure 3, 4, 5 繪圖
│
├── result/                        # 📁 結果輸出（運行時自動創建）
│   ├── analytical/                    # 解析結果 (CSV)
│   │   ├── figure1/{timestamp}/       # Figure 1 解析結果
│   │   ├── figure2/{timestamp}/       # Figure 2 解析結果
│   │   └── figure345/{timestamp}/     # Figure 3-5 解析結果
│   ├── simulation/                    # 模擬結果 (CSV)
│   │   └── figure345/{timestamp}/     # Figure 3-5 模擬結果
│   └── graph/                         # 圖表輸出 (PNG)
│       ├── figure1/{timestamp}/       # Figure 1 圖表
│       ├── figure2/{timestamp}/       # Figure 2 圖表
│       ├── figure3/{timestamp}/       # Figure 3 圖表
│       ├── figure4/{timestamp}/       # Figure 4 圖表
│       └── figure5/{timestamp}/       # Figure 5 圖表
│
├── docs/                          # 📚 文檔
│   ├── FYP-Paper-1.pdf                # 論文 PDF
│   └── Paper.md                       # 論文筆記
│
└── architecture/                  # 🏗️ 架構文檔
    ├── README.md                      # 架構文檔說明
    ├── structure/                     # 結構相關圖表
    ├── data_flow/                     # 數據流圖表
    ├── workflows/                     # 工作流程圖表
    └── formulas/                      # 公式依賴圖表
```

---

## 🚀 快速開始

### 環境要求

- **Python 3.14 (freethreaded)** - 必需使用 freethreaded 版本以支持真正的多線程並行（模擬模組使用 ThreadPoolExecutor 實現並行計算）
- uv (推薦) 或 pip

### 依賴套件

| 套件       | 版本      |
| ---------- | --------- |
| numpy      | == 2.3.5  |
| matplotlib | == 3.10.7 |
| scipy      | == 1.16.3 |
| tqdm       | == 4.67.1 |
| pyyaml     | == 6.0.3  |

### 安裝依賴

```bash
uv sync
```

### 運行方式

#### 1. 互動式選單（推薦）

```bash
uv run python main.py
```

顯示選單：

```
======================================================================
One-Shot Random Access 模擬與分析系統
======================================================================

請選擇操作:

【解析計算 (Analytical)】
   1. Figure 1: NS,1/N & NC,1/N 精確公式 + 近似公式
   2. Figure 2: 近似誤差分析（精確 vs 近似）
   3. Figure 3, 4, 5 合併解析 (P_S, T_a, P_C)
   4. 運行所有解析計算

【模擬 (Simulation)】
   5. Figure 3, 4, 5 合併模擬 (P_S, T_a, P_C)

【繪圖 (Plot)】
   6. 繪製 Figure 1
   7. 繪製 Figure 2
   8. 繪製 Figure 3, 4, 5
   9. 繪製所有圖表

【完整流程】
  10. Figure 1 完整流程 (Analytical + Plot)
  11. Figure 2 完整流程 (Analytical + Plot)
  12. Figure 3, 4, 5 完整流程 (Analytical + Simulation + Plot)
  13. 所有圖表完整流程

   0. 退出
======================================================================
```

#### 2. CLI 模式

```bash
# 解析計算
uv run python main.py analytical figure1        # 運行 Figure 1 解析
uv run python main.py analytical figure2        # 運行 Figure 2 解析
uv run python main.py analytical figure345      # 運行 Figure 3, 4, 5 解析
uv run python main.py analytical all            # 運行所有解析

# 模擬
uv run python main.py simulation figure345      # 運行 Figure 3, 4, 5 模擬

# 繪圖
uv run python main.py plot figure1              # 繪製 Figure 1
uv run python main.py plot figure2              # 繪製 Figure 2
uv run python main.py plot figure345            # 繪製 Figure 3, 4, 5
uv run python main.py plot all                  # 繪製所有圖表

# 完整流程 (解析 + 模擬 + 繪圖)
uv run python main.py run figure1               # Figure 1 完整流程
uv run python main.py run figure2               # Figure 2 完整流程
uv run python main.py run figure345             # Figure 3, 4, 5 完整流程
uv run python main.py run all                   # 所有完整流程
```

---

## 📊 各選項完整流程詳解

以下詳細說明每個選項從 `main.py` 開始的完整調用流程。

---

### 【選項 1】Figure 1 解析計算

**功能**: 計算 NS,1/N 和 NC,1/N 的精確公式與近似公式結果

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 調用流程                                                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  main.py                                                                    │
│    │                                                                        │
│    ├─► run_analytical_figure1()                                             │
│    │     │                                                                  │
│    │     ├─► load_config('analytical', 'figure1')                           │
│    │     │     └─► config/loader.py::load_config()                          │
│    │     │           └─► 讀取 config/analytical/figure1.yaml                │
│    │     │                                                                  │
│    │     └─► run_figure1_analysis(config)                                   │
│    │           └─► analytical/figure_analysis/figure1_analysis.py           │
│    │                 │                                                      │
│    │                 ├─► parallel_compute(compute_single_point, ...)        │
│    │                 │     └─► common.py::parallel_compute()                │
│    │                 │           └─► ThreadPoolExecutor 並行計算            │
│    │                 │                                                      │
│    │                 ├─► compute_single_point(M, N)                         │
│    │                 │     ├─► paper_formula_3_success_raos_exact(M, N)     │
│    │                 │     ├─► paper_formula_2_collision_raos_exact(M, N)   │
│    │                 │     ├─► paper_formula_4_success_approx(M, N)         │
│    │                 │     └─► paper_formula_5_collision_approx(M, N)       │
│    │                 │           └─► analytical/formulas.py                 │
│    │                 │                                                      │
│    │                 └─► save_figure1_results(results)                      │
│    │                       └─► 保存到 result/analytical/figure1/{時間戳}/   │
│    │                             ├─► figure1_N3.csv                         │
│    │                             └─► figure1_N14.csv                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**輸出文件**:
- `result/analytical/figure1/{timestamp}/figure1_N{n}.csv`
- 欄位: `M, M/N, analytical_N_S, analytical_N_C, approx_N_S, approx_N_C`

---

### 【選項 2】Figure 2 解析計算

**功能**: 計算精確公式與近似公式的誤差百分比

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 調用流程                                                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  main.py                                                                    │
│    │                                                                        │
│    ├─► run_analytical_figure2()                                             │
│    │     │                                                                  │
│    │     ├─► load_config('analytical', 'figure1')                           │
│    │     │     └─► config/loader.py::load_config()                          │
│    │     │           └─► 讀取 config/analytical/figure1.yaml (共用配置)    │
│    │     │                                                                  │
│    │     └─► run_figure2_analysis(config)                                   │
│    │           └─► analytical/figure_analysis/figure2_analysis.py           │
│    │                 │                                                      │
│    │                 ├─► run_figure1_analysis(config_no_save)               │
│    │                 │     └─► 先運行 Figure 1 獲取基礎數據（不保存）        │
│    │                 │                                                      │
│    │                 ├─► 計算誤差                                            │
│    │                 │     └─► error = |analytical - approx| / |analytical| │
│    │                 │                                                      │
│    │                 └─► save_figure2_results(error_results)                │
│    │                       └─► 保存到 result/analytical/figure2/{時間戳}/   │
│    │                             ├─► figure2_N3.csv                         │
│    │                             └─► figure2_N14.csv                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**輸出文件**:
- `result/analytical/figure2/{timestamp}/figure2_N{n}.csv`
- 欄位: `M, M/N, analytical_N_S, analytical_N_C, approx_N_S, approx_N_C, N_S_error(%), N_C_error(%)`

---

### 【選項 3】Figure 3, 4, 5 合併解析計算

**功能**: 使用理論公式計算 P_S, T_a, P_C

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 調用流程                                                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  main.py                                                                    │
│    │                                                                        │
│    ├─► run_analytical_figure345()                                           │
│    │     │                                                                  │
│    │     ├─► load_config('analytical', 'figure345')                         │
│    │     │     └─► config/loader.py::load_config()                          │
│    │     │           └─► 讀取 config/analytical/figure345.yaml              │
│    │     │                                                                  │
│    │     └─► run_figure345_analysis(config)                                 │
│    │           └─► analytical/figure_analysis/figure345_analysis.py         │
│    │                 │                                                      │
│    │                 ├─► for N in N_range:                                  │
│    │                 │     └─► theoretical_calculation(M, N, I_max)         │
│    │                 │           └─► analytical/theoretical.py              │
│    │                 │                 │                                    │
│    │                 │                 ├─► 迭代計算 I_max 個 AC 周期         │
│    │                 │                 │     ├─► paper_formula_6_*()        │
│    │                 │                 │     └─► paper_formula_7_*()        │
│    │                 │                 │                                    │
│    │                 │                 ├─► paper_formula_8_*() → P_S        │
│    │                 │                 ├─► paper_formula_9_*() → T_a        │
│    │                 │                 └─► paper_formula_10_*() → P_C       │
│    │                 │                       └─► analytical/formulas.py     │
│    │                 │                                                      │
│    │                 └─► save_figure345_results(results)                    │
│    │                       └─► result/analytical/figure345/{時間戳}/        │
│    │                             └─► figure345_analytical.csv               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**輸出文件**:
- `result/analytical/figure345/{timestamp}/figure345_analytical.csv`
- 欄位: `N, P_S, T_a, P_C, M, I_max`

---

### 【選項 4】運行所有解析計算

**功能**: 依序執行選項 1 → 2 → 3

```
main.py::run_analytical_all()
    ├─► run_analytical_figure1()    # 選項 1
    ├─► run_analytical_figure2()    # 選項 2
    └─► run_analytical_figure345()  # 選項 3
```

---

### 【選項 5】Figure 3, 4, 5 合併模擬

**功能**: 蒙特卡洛模擬計算 P_S, T_a, P_C，並計算與解析結果的誤差

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 調用流程                                                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  main.py                                                                    │
│    │                                                                        │
│    ├─► run_simulation_figure345()                                           │
│    │     │                                                                  │
│    │     ├─► load_config('simulation', 'figure345')                         │
│    │     │     └─► config/loader.py::load_config()                          │
│    │     │           └─► 讀取 config/simulation/figure345.yaml              │
│    │     │                                                                  │
│    │     └─► run_figure345_simulation(config)                               │
│    │           └─► simulation/figure_simulation/figure345_simulation.py     │
│    │                 │                                                      │
│    │                 ├─► for N in N_range:                                  │
│    │                 │     │                                                │
│    │                 │     ├─► simulate_group_paging_multi_samples(...)     │
│    │                 │     │     └─► simulation/core/group_paging.py        │
│    │                 │     │           │                                    │
│    │                 │     │           └─► 並行執行 num_samples 次模擬       │
│    │                 │     │                 └─► simulate_one_shot_access() │
│    │                 │     │                       └─► one_shot_access.py   │
│    │                 │     │                                                │
│    │                 │     └─► calculate_performance_metrics(results_array) │
│    │                 │           └─► simulation/core/metrics.py             │
│    │                 │                 └─► 計算均值和置信區間               │
│    │                 │                                                      │
│    │                 ├─► load_figure345_results()                           │
│    │                 │     └─► 讀取解析結果（用於計算誤差）                  │
│    │                 │                                                      │
│    │                 ├─► calculate_approximation_error(analytical, sim)     │
│    │                 │     └─► error = |analytical - sim| / |analytical|    │
│    │                 │                                                      │
│    │                 └─► save_figure345_simulation_results(results)         │
│    │                       └─► result/simulation/figure345/{時間戳}/        │
│    │                             └─► figure345_simulation.csv               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**輸出文件**:
- `result/simulation/figure345/{timestamp}/figure345_simulation.csv`
- 欄位: `N, P_S, T_a, P_C, P_S_error, T_a_error, P_C_error, M, I_max`

**注意**: 需要先運行選項 3（解析計算）才能計算 Approximation Error

---

### 【選項 6】繪製 Figure 1

**功能**: 從 CSV 讀取數據並繪製 Figure 1

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 調用流程                                                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  main.py                                                                    │
│    │                                                                        │
│    ├─► run_plot_figure1()                                                   │
│    │     │                                                                  │
│    │     ├─► load_figure1_results()                                         │
│    │     │     └─► analytical/figure_analysis/figure1_analysis.py           │
│    │     │           └─► 讀取 result/analytical/figure1/{最新時間戳}/       │
│    │     │                 └─► figure1_N*.csv                               │
│    │     │                                                                  │
│    │     ├─► get_result_dir('graph', 'figure1')                             │
│    │     │     └─► 建立 result/graph/figure1/{時間戳}/                       │
│    │     │                                                                  │
│    │     └─► plot_figure1(data, save_path=...)                              │
│    │           └─► plot/figure1.py::plot_figure1()                          │
│    │                 │                                                      │
│    │                 ├─► extract_n_values_from_data(data)                   │
│    │                 │     └─► plot/common.py                               │
│    │                 │                                                      │
│    │                 ├─► 創建 2x3 子圖佈局                                    │
│    │                 │     ├─► 上排: (a) N=3, (b) N=14, (c) Approximation   │
│    │                 │     └─► 下排: (d) All Combined                       │
│    │                 │                                                      │
│    │                 ├─► _setup_axis(ax)                                    │
│    │                 │     └─► 設定 X/Y 軸範圍、標籤、網格                   │
│    │                 │                                                      │
│    │                 └─► fig.savefig(save_path, dpi=300)                    │
│    │                       └─► result/graph/figure1/{時間戳}/figure1.png    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**前置條件**: 需要先運行選項 1（Figure 1 解析）
**輸出文件**: `result/graph/figure1/{timestamp}/figure1.png`

---

### 【選項 7】繪製 Figure 2

**功能**: 從 CSV 讀取數據並繪製誤差圖

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 調用流程                                                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  main.py                                                                    │
│    │                                                                        │
│    ├─► run_plot_figure2()                                                   │
│    │     │                                                                  │
│    │     ├─► load_figure2_results()                                         │
│    │     │     └─► analytical/figure_analysis/figure2_analysis.py           │
│    │     │           └─► 讀取 result/analytical/figure2/{最新時間戳}/       │
│    │     │                                                                  │
│    │     ├─► get_result_dir('graph', 'figure2')                             │
│    │     │                                                                  │
│    │     └─► plot_figure2(data, save_path=...)                              │
│    │           └─► plot/figure2.py::plot_figure2()                          │
│    │                 │                                                      │
│    │                 ├─► 創建 2 行佈局                                        │
│    │                 │     ├─► 上排: (a) N=3, (b) N=14                       │
│    │                 │     └─► 下排: (c) All Combined                       │
│    │                 │                                                      │
│    │                 ├─► Y 軸使用對數刻度 (log scale)                        │
│    │                 │                                                      │
│    │                 └─► fig.savefig(save_path, dpi=300)                    │
│    │                       └─► result/graph/figure2/{時間戳}/figure2.png    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**前置條件**: 需要先運行選項 2（Figure 2 解析）
**輸出文件**: `result/graph/figure2/{timestamp}/figure2.png`

---

### 【選項 8】繪製 Figure 3, 4, 5

**功能**: 繪製三張圖，每張包含理論曲線、模擬結果和誤差曲線

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 調用流程                                                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  main.py                                                                    │
│    │                                                                        │
│    ├─► run_plot_figure345()                                                 │
│    │     │                                                                  │
│    │     ├─► [Figure 3]                                                     │
│    │     │     ├─► _get_analytical_data_for_figure('figure3')               │
│    │     │     │     └─► load_figure345_results()                           │
│    │     │     │           └─► 讀取 analytical/figure345/figure345_*.csv    │
│    │     │     │                                                            │
│    │     │     ├─► _get_simulation_data_for_figure('figure3')               │
│    │     │     │     └─► load_figure345_simulation_results()                │
│    │     │     │           └─► 讀取 simulation/figure345/figure345_*.csv    │
│    │     │     │                 （包含 P_S_error）                          │
│    │     │     │                                                            │
│    │     │     └─► plot_figure3(analytical_data, simulation_data, ...)      │
│    │     │           └─► plot/figure345.py::plot_figure3()                  │
│    │     │                 ├─► 左 Y 軸: P_S（藍色）                          │
│    │     │                 │     ├─► 理論曲線（實線）                        │
│    │     │                 │     └─► 模擬點（空心圓）                        │
│    │     │                 └─► 右 Y 軸: Approximation Error（綠色虛線）     │
│    │     │                                                                  │
│    │     ├─► [Figure 4] (同樣流程，使用 T_a 數據)                            │
│    │     │     └─► plot_figure4(...)                                        │
│    │     │                                                                  │
│    │     └─► [Figure 5] (同樣流程，使用 P_C 數據)                            │
│    │           └─► plot_figure5(...)                                        │
│    │                                                                        │
│    │     輸出:                                                               │
│    │       ├─► result/graph/figure3/{時間戳}/figure3.png                    │
│    │       ├─► result/graph/figure4/{時間戳}/figure4.png                    │
│    │       └─► result/graph/figure5/{時間戳}/figure5.png                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**前置條件**: 
- 選項 3（解析計算）- 提供理論曲線
- 選項 5（模擬）- 提供模擬點和誤差曲線

---

### 【選項 9】繪製所有圖表

```
main.py::run_plot_all()
    ├─► run_plot_figure1(show=False)   # 選項 6
    ├─► run_plot_figure2(show=False)   # 選項 7
    ├─► run_plot_figure345(show=False) # 選項 8
    └─► plt.show()                     # 最後一起顯示
```

---

### 【選項 10】Figure 1 完整流程

**功能**: Analytical → Plot

```
main.py::run_pipeline_figure1()
    │
    ├─► [1/2] run_analytical_figure1()
    │         └─► 執行選項 1 流程
    │
    └─► [2/2] run_plot_figure1()
              └─► 執行選項 6 流程
```

---

### 【選項 11】Figure 2 完整流程

**功能**: Analytical → Plot

```
main.py::run_pipeline_figure2()
    │
    ├─► [1/2] run_analytical_figure2()
    │         └─► 執行選項 2 流程
    │
    └─► [2/2] run_plot_figure2()
              └─► 執行選項 7 流程
```

---

### 【選項 12】Figure 3, 4, 5 完整流程

**功能**: Analytical → Simulation → Plot

```
main.py::run_pipeline_figure345()
    │
    ├─► [1/3] run_analytical_figure345()
    │         └─► 執行選項 3 流程
    │
    ├─► [2/3] run_simulation_figure345()
    │         └─► 執行選項 5 流程（自動計算誤差）
    │
    └─► [3/3] run_plot_figure345()
              └─► 執行選項 8 流程
```

---

### 【選項 13】所有圖表完整流程

```
main.py::run_pipeline_all()
    ├─► run_pipeline_figure1()   # 選項 10
    ├─► run_pipeline_figure2()   # 選項 11
    └─► run_pipeline_figure345() # 選項 12
```

---

## 📐 論文公式對應

| 公式   | 函數                                         | 說明                    | 使用於      |
| ------ | -------------------------------------------- | ----------------------- | ----------- |
| Eq. 1  | `paper_formula_1_pk_probability`             | k 個碰撞 RAO 的概率     | Eq. 2, 3    |
| Eq. 2  | `paper_formula_2_collision_raos_exact`       | 期望碰撞 RAO 數（精確） | Figure 1    |
| Eq. 3  | `paper_formula_3_success_raos_exact`         | 期望成功 RAO 數（精確） | Figure 1    |
| Eq. 4  | `paper_formula_4_success_approx`             | 成功 RAO 近似公式       | Figure 1, 2 |
| Eq. 5  | `paper_formula_5_collision_approx`           | 碰撞 RAO 近似公式       | Figure 1, 2 |
| Eq. 6  | `paper_formula_6_success_per_cycle`          | 第 i 個 AC 成功設備數   | Figure 3-5  |
| Eq. 7  | `paper_formula_7_next_contending_devices`    | 下一個 AC 競爭設備數    | Figure 3-5  |
| Eq. 8  | `paper_formula_8_access_success_probability` | 接入成功概率 P_S        | Figure 3    |
| Eq. 9  | `paper_formula_9_mean_access_delay`          | 平均接入延遲 T_a        | Figure 4    |
| Eq. 10 | `paper_formula_10_collision_probability`     | 碰撞概率 P_C            | Figure 5    |

### 公式分類

- **Eq. 1-3**: 單周期精確公式（用於 Figure 1）
- **Eq. 4-5**: 單周期近似公式（用於 Figure 1, 2）
- **Eq. 6-7**: 多周期迭代公式
- **Eq. 8-10**: 性能指標公式（用於 Figure 3, 4, 5）

---

## ⚙️ 配置說明

配置文件位於 `config/` 目錄下，使用 YAML 格式。

### Figure 1 配置 (`config/analytical/figure1.yaml`)

**注意**: Figure 2 共用此配置文件，因為兩者基於相同的運算。

```yaml
# N 值列表 (論文: N=3 and 14)
n_values: [3, 14]

# M 範圍係數 (論文: M up to 10N)
m_over_n_max: 10

# M 起始值 (論文: M ranging from 1)
m_start: 1

# 並行計算核心數 (-1 表示使用所有核心)
n_jobs: -1
```

### Figure 3, 4, 5 解析配置 (`config/analytical/figure345.yaml`)

```yaml
# 設備總數
M: 100

# 最大接入周期數
I_max: 10

# N 範圍設定
N_start: 5
N_stop: 46
N_step: 1
```

### Figure 3, 4, 5 模擬配置 (`config/simulation/figure345.yaml`)

```yaml
description: "Figure 3, 4, 5: Combined Simulation (P_S, T_a, P_C)"

simulation:
  M: 100                     # 設備總數
  I_max: 10                  # 最大接入周期數

scan:
  parameter: N               # 掃描參數
  range:
    start: 5
    stop: 46
    step: 1

performance:
  num_samples: 10000000      # 樣本數量 10^7（論文要求）
  num_workers: -1            # 並行線程數 (-1 表示使用所有 CPU 核心)

output:
  save_csv: true
```

**性能優化**: 
- 模擬模組使用 Python 3.14 freethreaded 實現真正的多線程並行
- 詳細的模擬優化技術（批量隨機數生成、Thread-local RNG、滑動窗口等）請參考 [`SIMULATION_OPTIMIZATION.md`](SIMULATION_OPTIMIZATION.md)
- 當前性能：~70,000-74,000 樣本/秒，10^7 樣本預計耗時約 140-145 秒

### 單點測試配置 (`config/simulation/single_point.yaml`)

用於詳細分析單個參數組合的模擬配置。

```yaml
description: "Single Point Detailed Simulation"

simulation:
  M: 100                     # 設備總數
  N: 40                      # RAO 數量
  I_max: 10                  # 最大接入周期數

performance:
  num_samples: 1000          # 樣本數量（較少，用於快速測試）
  num_workers: 16            # 並行線程數

output:
  save_csv: true
  show_details: true         # 顯示詳細過程
```

**用途**: 用於開發和調試階段，快速驗證單個參數組合的模擬結果。

---

## 📁 結果輸出結構

**注意**: `result/` 目錄在首次運行時會自動創建，無需手動創建。所有結果文件按時間戳組織，便於版本管理和對比。

### CSV 文件

```
result/
├── analytical/
│   ├── figure1/{timestamp}/
│   │   ├── figure1_N3.csv      # 欄位: M, M/N, analytical_N_S, analytical_N_C, approx_N_S, approx_N_C
│   │   └── figure1_N14.csv
│   ├── figure2/{timestamp}/
│   │   ├── figure2_N3.csv      # 欄位: M, M/N, ..., N_S_error(%), N_C_error(%)
│   │   └── figure2_N14.csv
│   └── figure345/{timestamp}/
│       └── figure345_analytical.csv   # 欄位: N, P_S, T_a, P_C, M, I_max
│
└── simulation/
    └── figure345/{timestamp}/
        └── figure345_simulation.csv   # 欄位: N, P_S, T_a, P_C, P_S_error, T_a_error, P_C_error, M, I_max
```

### 圖表輸出

繪製的圖表會保存到 `result/graph/` 目錄下，每個圖表都有獨立的時間戳目錄：

```
result/graph/
├── figure1/{timestamp}/figure1.png
├── figure2/{timestamp}/figure2.png
├── figure3/{timestamp}/figure3.png
├── figure4/{timestamp}/figure4.png
└── figure5/{timestamp}/figure5.png
```

---

## 🔧 開發指南

### 添加新的 Figure

1. 在 `config/analytical/` 或 `config/simulation/` 添加配置文件
2. 在 `analytical/figure_analysis/` 或 `simulation/figure_simulation/` 添加計算邏輯
3. 在 `plot/` 添加繪圖函數
4. 更新 `main.py` 中的選單和處理邏輯

### 代碼結構原則

- **解耦**: Analytical、Simulation、Plot 三個模組獨立運作
- **數據流**: 所有數據通過 CSV 文件傳遞，不依賴記憶體
- **誤差計算**: Approximation Error 在 Simulation 完成後立即計算並保存

### 文檔維護要求

**⚠️ 重要規則**: 任何功能、架構、寫法更新必須在工作結束後更新相關目錄的子文檔。這是強制性要求，違反此規則將導致文檔與代碼不一致。

#### 文件夾文檔要求

每個文件夾都應包含一個極簡的架構說明（3行以內），列出：
- 文件夾的整體功能和地位
- 每個文件的名字、地位、功能

文件夾中的 README 或架構說明文件應聲明：**一旦我所屬的文件夾有所變化，請更新我。**

#### 文件註釋要求

每個文件開頭應包含三行極簡註釋：
- **Input（輸入）**: 依賴外部的什麼（模組、配置、數據等）
- **Output（輸出）**: 對外提供什麼（函數、類、數據等）
- **Position（地位）**: 在系統局部的地位是什麼（模組角色、功能定位等）

文件開頭應聲明：**一旦我被更新，務必更新我的開頭註釋，以及所屬文件夾的文檔。**

**注意**: 所有文件夾都必須有 README.md 架構說明，所有 Python 文件都必須有頭部三行註釋。這是項目規範的一部分。

### 架構文檔

項目包含完整的架構文檔，位於 `architecture/` 目錄。這些文檔使用 Mermaid 語法繪製，支持雙語標籤（英文/中文），幫助理解項目的整體架構和數據流動。

**架構文檔組織結構**：

- **結構圖表** (`structure/`): 
  - 目錄結構 (`directory/`): 完整的目錄樹狀圖
  - 模組架構 (`module/`): 模組關係和依賴圖
  - 括號圖 (`brace_maps/`): 層次結構的括號表示

- **數據流圖表** (`data_flow/`): 
  - 輸入輸出圖 (`input_output/`): 從 YAML 配置到 PNG 輸出的完整數據流
  - 數據流動圖 (`flow/`): 數據在系統中的流動方式

- **工作流程圖表** (`workflows/`): 
  - 概覽 (`overview/`): 完整管道工作流程的高層次概覽
  - 各階段 (`phases/`): 解析、模擬、繪圖、管道各階段的詳細工作流程
  - 各圖表 (`figures/`): Figure 1、2、3-5 的特定工作流程
  - 括號圖 (`brace_maps/`): 工作流程結構的括號表示

- **公式依賴圖表** (`formulas/`): 
  - 論文 10 個公式之間的依賴關係圖

**導航建議**：
- 從 [完整管道工作流程](architecture/workflows/overview/complete_pipeline_workflow.md) 開始了解高層次概覽
- 查看 [目錄樹狀圖](architecture/structure/directory/directory_tree.md) 了解項目結構
- 查看 [模組架構圖](architecture/structure/module/module_architecture_diagram.md) 了解模組關係
- 探索 [工作流程階段](architecture/workflows/phases/) 查看詳細階段工作流程
- 查看 [圖表工作流程](architecture/workflows/figures/) 了解圖表特定執行流程

詳細說明請參考 [`architecture/README.md`](architecture/README.md)。

---

## 📄 License

MIT License

