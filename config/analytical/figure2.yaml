# Figure 2 解析計算配置
# 近似誤差分析 - 基於 Figure 1 數據

# N 值列表 (與 Figure 1 相同)
n_values: [3, 6]

# M 範圍係數 (與 Figure 1 相同)
m_over_n_max: 10

# M 起始值 (與 Figure 1 相同)
m_start: 1

# 並行計算核心數 (-1 表示使用所有核心)
n_jobs: -1
