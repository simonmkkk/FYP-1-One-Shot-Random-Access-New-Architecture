# ============================================================================
# Config: Figure 1
# ============================================================================
# NS,1/N & NC,1/N vs M/N — exact + approximate formulas
# ============================================================================

name: figure1
description: "Figure 1: NS,1/N & NC,1/N exact + approximate (N=3,14)"

# Analysis type
figure_type: figure1

# N values (paper: N = 3 and 14)
n_values: [3, 14]

# M range coefficient (paper: M up to 10N)
m_over_n_max: 10

# M start value (paper: M from 1)
m_start: 1

# Parallel workers (-1 = all CPU cores)
n_jobs: -1
